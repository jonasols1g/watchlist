# Utviklingslogg

Kort logg over hva som er gjort per dag. Nyeste øverst. Én oppføring per dag det er gjort arbeid — noen linjer, ikke en rapport. Detaljene hører hjemme i dokumentene i `docs/`; her står bare hva som skjedde og hva som er neste steg.

## 2026-07-19

- **Slack-varsling for agent-arbeidsflyten** (`docs/plans/slack-varsling-subagenter.md`) satt i gang og løst via issue #34 → PR #35, full agent-loop (`dev` → `reviewer` godkjent uten funn → `verifier` «bestått med forslag») → squash-merge. Nytt script `scripts/notify-slack.mjs` POSTer til en delt Slack Incoming Webhook, med egne visningsnavn/ikoner per avsender (Orkestrator Ole, Planlegger Pia, Utvikler Ulrik, Gransker Guri, Godkjenner Gunnar). Ettlinjers varslingstillegg lagt inn i alle fire agent-filer og i `CLAUDE.md` sin "Agent-arbeidsflyt".
- Webhooken var allerede opprettet av bruker og `SLACK_WEBHOOK_URL` satt i den gitignorerte `.claude/settings.local.json` før implementasjonen startet — dev-agentens isolerte worktree hadde derfor ikke tilgang til den (filen er utenfor versjonskontroll), så `permissions.allow`-regelen for `Bash(node scripts/notify-slack.mjs:*)` og selve leveransetesten mot ekte Slack ble fullført manuelt av hovedsamtalen etter merge — alle fem avsendere sendte uten feil.
- **Full trial-runde kjørt samme dag** på et nytt, trivielt issue (#36, "Legg til description i package.json" — opprettet direkte i Ready jf. `CLAUDE.md`s unntak for trivielle oppgaver): hele flyten issue → `dev` (PR #37) → `reviewer` (godkjent) → `verifier` (bestått) → squash-merge → Done kjørt med ekte Slack-varsling fra agent-filenes egne instrukser (ikke bare hovedsamtalens orkestrator-meldinger), alle kall ga exit 0 uten feil.
- **Reelt funn fra trial-runden:** meldingene ankom i Slack, men under appens egen faste identitet ("agents-chat-app", standardikon) — ikke de fem persona-navnene/emojiene. Rotårsak: Slack ignorerer stille `username`/`icon_emoji` i JSON-bodyen for Incoming Webhooks tilknyttet en Slack-app (kun gamle, utfasede "legacy custom integration"-webhooks respekterer disse feltene). Fikset via issue #38 → PR #39, samme agent-loop (`dev` → `reviewer` godkjent → `verifier` bestått) → squash-merge: `scripts/notify-slack.mjs` bygger nå avsenderidentiteten som et emoji+fett-navn-prefiks inne i selve `text`-feltet i stedet. Bekreftet i Slack av bruker etter merge. `docs/plans/slack-varsling-subagenter.md` sin statuslinje er dermed satt til "fullført".
- **Feedback-innsending planlagt og delvis implementert** (`docs/plans/feedback-innsending-og-automatisk-oppfolging.md`, plan utarbeidet sammen med bruker i Plan Mode, committet direkte på `main`). Del A (ny, bevisst skjult `/feedback`-side — tekst + score 1–5, lagret i en ny, ikke-brukerbundet Firestore-collection `feedback/{autoId}`) løst via issue #40 → PR #41, full agent-loop (`dev` → `reviewer` godkjent uten funn → `verifier`).
- **Reelt funn ved verifisering:** samme klasse feil som for issue #16/#18/#19 — den nye `feedback`-matchen i `firestore.rules` var kun committet i repoet, aldri deployet til det faktiske `watchlist-jonasols1g`-prosjektet, så ekte submit ga `403`. Deployet manuelt av hovedsamtalen (`npx firebase-tools deploy --only firestore:rules`, midlertidig lokalt oppsett utenfor repoet) etter bekreftelse fra bruker; re-verifisering deretter bestått (ekte submit ga `200`, dokument bekreftet i Firestore med korrekte felt, testdata ryddet opp). PR #41 squash-merget, kort flyttet til Done.
- Del B (daglig skyagent-routine som vurderer ny feedback mot docs/ og selv åpner PR for konkrete forbedringsforslag, uten å merge autonomt) er **ikke satt opp ennå** — neste steg, jf. planen.
- **Første kjøring av Del B-routinen** (feedback-skyagent): hentet 1 dokument fra `feedback`-collectionen (ID `X1jHuyA76USsUVZRzNPQ`, score 4, forespørsel om tegn-teller på `/feedback`-siden). Ingen duplikater fra før. Vurdert som konkret og byggbart innenfor rammene → issue #42 opprettet, implementert (tegn-teller under tilbakemeldingsfeltet, ny enhetstest, lint/tester/build grønne) og PR #43 (`Closes #42`) åpnet mot `main`. PR-en er **ikke** merget — det gjøres av et menneske etter review/verifisering, jf. routinens harde grense.
  - Kjent begrensning i denne kjøringen: `gh`-CLI er ikke tilgjengelig i skyagent-miljøet, og GitHub-prosjektboardets GraphQL-mutasjoner er blokkert for økten (kun et fastsatt sett PR-review-operasjoner er proxyet). Issue #42 er derfor **ikke** lagt til på prosjektboardet eller flyttet gjennom Backlog/Ready/In progress/In review — dette må gjøres manuelt.

## 2026-07-18

- Spisset instruksjonene til de fire sub-agentene (`dev`, `reviewer`, `verifier`, `feature-planner`) i `.claude/agents/` for lavere token- og testkostnad, committet direkte på `main` (ingen kode rørt): docs-lesing målrettes med `grep -n "^#"` mot overskrifter i stedet for full-fil-lesing, `dev` kjører målrettede tester under iterasjon og full `npm test` kun rett før push, CI-logger hentes bare for feilende sjekker (ikke grønne), `verifier` kan nå hoppe over den manuelle flyten for rene docs-/test-differ, og `feature-planner` filtrerer åpne issues på nøkkelord fremfor å lese alle i sin helhet.

- Fase 11 (visuelt redesign, CineFind-tema) lagt til i faseplanen og implementert samme dag. Bruker leverte en tekstlig design-spec med fire hifi-skjermbilder (`docs/design-spec/`) — en statisk HTML-mockup som skulle gjenskapes i den eksisterende React/Tailwind-stacken, ikke kopieres direkte. Bruker ba eksplisitt om å hoppe over `feature-planner`, siden dette er en restyling av allerede planlagte sider (fase 5–9), ikke en ny feature.
- Faseplan og designdokumentasjon oppdatert direkte på `main` før implementasjon (jf. praksis fra PR #10): `docs/design.md` fikk en ny seksjon som oppsummerer fargepalett/typografi/hue-mekanikk, `docs/dev-tasks.md` fikk fase 11s oppgaveliste.
- Implementert via PR #11 med full agent-loop: `dev` → `reviewer` (godkjent uten funn) → `verifier` (grønn CI + drev hele flyten manuelt mot produksjonsbygget: tom-tilstand → søk → søkeresultater → detaljside → legg til i watchlist → watchlist-siden, sammenlignet visuelt mot alle fire referanseskjermbildene) → squash-merge.
- Nøkkelbeslutning: per-tittel "hue" (aksentfarge på plakat-ring/meta-tekst/stjerne) er implementert som en ren UI-lags hash-funksjon (`utils/accentHue.ts`, deterministisk fra `id`), **ikke** et nytt felt på `Media`/`MediaSummary` — holder `MediaProvider`-kontrakten og datamodellen uendret foran fase 10.
- Google Fonts (Space Grotesk, Manrope) krevde en liten, målrettet utvidelse av CSP-en (`style-src`/`font-src` for `fonts.googleapis.com`/`fonts.gstatic.com`), fortsatt kun injisert ved build — verifisert at ingen andre direktiver ble svekket og at ingen ekte API-kall skjer.
- Seks bevisste, mindre avvik fra pixel-perfect (NavBar-tekst, watchlist-tittel, stjerne-toggles skop, ekstra statuslenke, hero som gjenbruker `posterUrl`, hero-breakout begrenset til `<main>`) ble vurdert og godkjent av både `reviewer` og `verifier` — alle bevarer eksisterende, testdekket funksjonalitet fra fase 5–9. Se detaljene i `docs/dev-tasks.md` under fase 11.
- Ingen nye API-kall, ingen endring i MOTN/OMDb-forberedelsene for fase 10.

- Pages-beslutningen tatt: bruker valgte å gjøre repoet `jonasols1g/watchlist` **offentlig** (fremfor GitHub Pro), bekreftet via `gh repo edit --visibility public`. Løser opp både fase 9s utsatte punkt og fase 10s Pages-oppgave.
- Fase 10 (ekte API-integrasjon) satt i gang samme dag, med ett bevisst avvik fra oppgavelisten: bruker valgte å starte implementasjonen **før** OMDb-/MOTN-nøkler er skaffet (nøkkelanskaffelse er en oppgave kun bruker kan gjøre). Alt annet i fasen ble gjennomført via PR #12 med full agent-loop: `dev` → `reviewer` (godkjent uten funn) → `verifier` (grønn CI, 212 enhetstester + 11 E2E-tester, manuell kjøring mot produksjonsbygg — bekreftet bl.a. at et 401-svar fra OMDb pga. manglende nøkkel gir tydelig feilmelding i UI og logging i konsollen, akkurat som DoD krever) → squash-merge.
- Implementert: `OmdbMediaProvider`, `MotnMediaProvider`, `CompositeMediaProvider` med all mapping/feilhåndtering/URL-validering fra `architecture.md`, utvidet CSP, `Footer` med attribusjon, `services/media/index.ts` byttet til `CompositeMediaProvider`, data-versjon bumpet til `v2`, og en ny `deploy`-jobb i CI-workflowen (build → `configure-pages` → `upload-pages-artifact` → `deploy-pages`, gatet på push til `main`). `.env.example` dokumenterer de to nødvendige miljøvariablene.
- Reelt funn underveis (fikset av `dev` i samme runde, ikke en review-runde): delt `beforeEach`-modul for E2E-stubbing var flaky under parallellisering — ES-modulcache gjorde at kun første spec-fil per worker fikk stubbingen, resten gjorde ekte (feilende) kall mot `omdbapi.com`. Løst ved at hver spec-fil kaller `registerApiStubs(page)` i sin egen `beforeEach`.
- Bruker leverte ekte OMDb-/MOTN-nøkler samme dag. Lagt i `.env.local` og som GitHub Actions-secrets (`gh secret set`); Pages Source satt til «GitHub Actions» via `gh api repos/.../pages -f build_type=workflow». Begge nøkler verifisert direkte med `curl` mot OMDb/MOTN v4 før videre arbeid.
- **Reelt funn ved første produksjonsverifisering:** live-siden ga 401 fra OMDb ved søk — nettverksinspeksjon viste `apikey=` (tom) i det faktiske kallet. Rotårsak: CI-kjøringen som deploy'et var trigget (av en dokumentasjons-commit) *før* secretsene ble opprettet i repoet, så build-steget fikk tomme miljøvariabler selv om selve jobben kjørte flere minutter senere — GitHub Actions ser ut til å låse secret-konteksten ved kjøringens start, ikke ved det enkelte steget som leser den. Fiks: `gh run rerun` på samme commit (ingen kodeendring nødvendig) ga en ny kjøring som bakte inn nøklene korrekt, bekreftet både med `curl` mot det bygde JS-bundelet og med en full `verifier`-runde i nettleser.
- Endelig verifisering (`verifier`, mot `https://jonasols1g.github.io/watchlist/`): søk på «Matrix» ga ekte OMDb-treff, detaljside for `tt0133093` viste ekte IMDb-score (8.7), RT-score (83 %), sjangre og MOTN-strømmetilbud (Apple TV, Prime Video), og en tittel uten strømmetilbud i Norge viste korrekt tom-tilstanden «Ingen strømmetjenester funnet for din region». Dyplenke direkte til `/title/tt0133093` + `page.reload()` bekreftet på nytt i produksjon (fase 9s siste DoD-punkt).
- Fase 9 og fase 10 er dermed begge hakket av som ferdige i `docs/dev-tasks.md`.

- **Oppgavesporing flyttet til GitHub Projects.** `docs/dev-tasks.md` arkivert til `docs/archive/dev-tasks.md` (alle 11 faser ferdige, ingen flere oppgaver å spore der). Fase 11s liste over bevisste pixel-avvik flyttet til `docs/design.md` før arkivering. Agent-arbeidsflyten i `CLAUDE.md` og agent-instruksene for `dev`/`feature-planner`/`reviewer` skrevet om til å bruke GitHub-issues + boardets `Status`-felt (Backlog/Ready/In progress/In review/Done) i stedet for avhuking i `dev-tasks.md`. `dev-log.md` uendret — føres fortsatt som før.
- Flyten testet ende-til-ende på en reell, liten oppgave (issue #14 → PR #15, fjernet en utdatert "faseplan"-referanse fra rot-`README.md`): `dev` → `reviewer` (godkjent) → `verifier`. Verifier fant et reelt problem reviewer hadde oversett: den nye README-lenken pekte til GitHub-prosjektet, som var **privat** mens repoet er offentlig — ekstern lenke ga 404. Bruker valgte å gjøre prosjektet offentlig (`gh project edit 2 --owner jonasols1g --visibility PUBLIC`) fremfor å fjerne lenken; verifier bekreftet fiksen i en ny runde (HTTP 200 uautentisert), deretter squash-merge og issue lukket automatisk via `Closes #14`.

- **DB-migrering (localStorage → Firebase/Firestore) satt i gang**, jf. `docs/plans/watchlist-database-migrering.md`. Issue-oppdelingen fra planen opprettet som issues #16–#20 (A–E) i prioritert rekkefølge på boardet, kun #16 satt til Ready ved opprettelse siden resten er strengt avhengige av forgjengeren sin.
- **Issue #16 (Firebase-prosjekt, Firestore-oppsett og klientkonfigurasjon) løst via PR #21**, full agent-loop (`dev` → `reviewer` godkjent uten funn → `verifier` grønt) → squash-merge. Kodedelen: `firebase`-avhengighet, `src/services/auth/firebaseClient.ts` (samme sammensetningsrot-mønster som `services/media/index.ts`), `firestore.rules`, utvidet `.env.example`/CSP, `experimentalForceLongPolling: true` med dokumentert (ikke ekte-testet) vurdering.
- Den manuelle Firebase-forutsetningen gjort av hovedsamtalen via `npx firebase-tools` (installasjon ikke nødvendig, kjørt via `npx`): prosjekt `watchlist-jonasols1g` opprettet, web-app registrert, Firestore-database opprettet (region `nam5`, standard lukkede regler), Anonymous-innlogging aktivert i konsollen (ingen CLI-kommando finnes for dette). Alle seks `VITE_FIREBASE_*`-verdier lagt i `.env.local` og som GitHub Actions-secrets, likt mønsteret for OMDb/MOTN.
- Merk til neste issue (#17, AuthContext): `firebaseClient.ts` importeres ennå ikke noe sted, og `ci.yml` sender foreløpig ikke `VITE_FIREBASE_*`-secretsene til byggesteget — begge deler må kobles inn når Firebase faktisk tas i bruk i runtime.
- **Issue #17 (AuthContext med usynlig anonym innlogging) løst via PR #22**, full agent-loop, tre review-runder før godkjenning:
  - Runde 1: `App.test.tsx` mocket ikke Firebase Auth, og `AuthContext`s import av `firebaseClient.ts` trigget ekte `getAuth()`-init i denne testfila i CI (uten secrets) → `auth/invalid-api-key` felte suiten. Fikset ved å mocke `firebase/auth`/`firebaseClient` i `App.test.tsx`, samme mønster som `AuthContext.test.tsx`.
  - Runde 2 (alvorlig): reviewer fant at rotårsaken var dypere — nettopp fordi `firebaseClient.ts`s `getAuth()` kaster synkront ved tom/ugyldig config, ville hele `App`-treet feile å montere i både `e2e`- og `deploy`-jobbene i `ci.yml`, som ikke sendte `VITE_FIREBASE_*`-secretsene til byggestegene (kun `test`-jobben fikk dem, men bruker dem ikke). Ville ha krasjet den ekte, live GitHub Pages-appen om det ble merget. Fiks: koblet secretsene (som allerede fantes i repoet fra issue #16) inn i `e2e`- og `deploy`-jobbenes bygg-/testkjøring. Under verifisering av denne fiksen oppdaget `dev` selv et beslektet problem: med ekte config satt gjorde `AuthContext` et ekte `signInAnonymously`-kall mot Firebase ved hver testkjøring — opprettet reelle anonyme brukere i produksjonsprosjektet. Fikset med ny `e2e/fixtures/firebaseAuthStub.ts` som stubber `identitytoolkit.googleapis.com`, koblet inn i alle spec-filers `beforeEach` (inkl. `smoke.spec.ts`, som tidligere ikke stubbet noe).
  - Runde 3: godkjent. `verifier` bekreftet grønn CI i skyen og drev flyten manuelt mot produksjonsbygget (ekte anonym sesjon opprettes usynlig ved første besøk, overlever reload, ingen UI-endring, ingen konsollfeil).
  - Sidemerknad fra verifier: hver manuell verifisering mot ekte Firebase oppretter en reell anonym testbruker i `watchlist-jonasols1g` — ikke blokkerende, men verdt å vurdere opprydding hvis det gjentas ofte.

- **Issue #18 (DB-migrering C: WatchlistStorage-abstraksjon mot Firestore, kjernebyttet) løst via PR #23**, full agent-loop, to review-runder før godkjenning. Den klart mest omfattende issuen i A–E-rekken.
  - Kodedelen: nytt grensesnitt `WatchlistStorage` (fil hetende `WatchlistRemoteStorage.ts` — kolliderer byte-for-byte med eksisterende `watchlistStorage.ts` på macOS' case-insensitive filsystem, godkjent avvik), `FirestoreWatchlistStorage`, `WatchlistContext` bygget om til optimistic update + write-through mot Firestore med `localStorage` som offline-skriveputt (offentlig Context-API uendret), ny testdobbel og E2E-Firestore-stubbing.
  - **Reelt arkitekturfunn midtveis:** issue As antagelse om at `experimentalForceLongPolling: true` ville gjøre trafikken diskret HTTP-stubbar, ble avkreftet empirisk mot det ekte Firebase-prosjektet — full `firebase/firestore`-klienten bruker fortsatt en stateful WebChannel-protokoll. Løst ved å bytte `firebaseClient.ts`/`FirestoreWatchlistStorage.ts` til `firebase/firestore/lite` (rendyrket REST, ett diskret kall per operasjon) — bifangst: bundlestørrelsen falt fra 830 kB til 464 kB. `firebase/auth` uendret (full SDK). Dokumentert i `docs/plans/watchlist-database-migrering.md`. Reviewer vurderte og godkjente avviket.
  - **Runde 1 (reviewer):** to reelle race conditions i write-through-/hydreringslogikken. (1) Rollback ved feilet Firestore-skriving brukte et globalt snapshot tatt ved kalltidspunktet, som kunne slette en senere, uavhengig, vellykket handling. (2) Initial Firestore-henting overskrev ubetinget lokal state, uten hensyn til handlinger brukeren gjorde mens hentingen var underveis. Fikset med presis `computeUndoPatch`/`computeRedoPatch`-logikk (reverserer/spiller av kun den aktuelle handlingens egen effekt) + 5 nye enhetstester (232 totalt).
  - **Runde 2:** godkjent.
  - **Manuell forutsetning håndtert av hovedsamtalen:** `firestore.rules` (uendret siden issue A) var aldri faktisk deployet til `watchlist-jonasols1g` — kun i repoet. Blokkerte all ekte Firestore-verifisering (`403 PERMISSION_DENIED`). Deployet manuelt med `npx firebase-tools deploy --only firestore:rules` (midlertidig lokal `firebase.json` opprettet og fjernet igjen — ikke en del av repoet) etter bekreftelse fra bruker, siden dette er en produksjonspåvirkende handling utenfor dev-agentens sandkasse.
  - `verifier` drev hele flyten manuelt mot produksjonsbygget med ekte Firebase-prosjekt: la til, satte status, fjernet en tittel — bekreftet med nettverkstrace at riktige Firestore-kall gikk til riktig `users/{uid}/watchlistItems`-sti. Strengeste test: tømte `localStorage` helt og reloadet — watchlisten kom likevel tilbake korrekt fra Firestore. Testdata ryddet opp i det ekte prosjektet etterpå.

- **Issue #19 (DB-migrering D: migrering av eksisterende localStorage-watchlist) løst via PR #24**, full agent-loop, to review-runder før godkjenning.
  - Kodedelen: `migrateLocalWatchlistToCloud.ts` — engangs migrering ved første hydrering, lokal versjon vinner ved konflikt, migreringsflagg settes kun ved bekreftet suksess (også i det trivielle "tom lokal watchlist"-tilfellet, se funn under), lokale data slettes aldri før bekreftet vellykket opplasting.
  - **Reelt funn dev selv fanget og fikset underveis:** en naiv første versjon satte kun flagget ved vellykket opplasting av *noe*, som gjorde at migrering ble forsøkt på nytt ved hvert app-load når lokal watchlist var tom ved første hydrering — og da kunne lese en potensielt eldre `localStorage`-verdi som overskrev korrekt, nyere Firestore-status. Fikset ved at flagget nå settes også ved tomt lokalt utgangspunkt, som gjør sjekket til en ekte engangshendelse.
  - **Runde 1 (reviewer):** samme race-condition-klasse som i issue #18 reintrodusert i et nytt vindu — `hydrate()` tømte `isHydratingRef`/`pendingPatchesRef` *før* migreringens egen Firestore-opplasting var ferdig, så brukerhandlinger midt i opplastingsvinduet ble stille overskrevet av migreringens avsluttende `setItems(merged)`. Fikset ved at `isHydratingRef` nå holdes sann gjennom hele hydrerings-/migreringsvinduet, med patcher fra `load()`- og migreringsvinduet spilt av separat og i riktig rekkefølge.
  - **Runde 2:** godkjent. Et gjenstående, svært smalt multi-fane-race i selve migreringsfunksjonen (ingen `await` mellom flagg-lesing og flagg-setting) ble bevisst latt stå — verste konsekvens er et idempotent duplisert `upsert`, ikke datatap, og "flere faner" er allerede en dokumentert v1-begrensning i planen.
  - `verifier` seedet en fersk nettleserprofil med kjente IMDb-ID-er i `localStorage`, bekreftet via nettverkslogg og direkte Firestore-REST-kall at begge dukket opp korrekt i `users/{uid}/watchlistItems`, og at et påfølgende reload verken duplisert eller gjorde nye skrivinger. Testdata ryddet opp i det ekte prosjektet etterpå.

- **Issue #20 (DB-migrering E: dokumentasjonskonsolidering) løst via PR #25**, ren dokumentasjonsendring (ingen kildekode), godkjent av `reviewer` uten funn, `verifier` bestått med to ikke-blokkerende forslag utenfor scope (manglende E2E-filer i arkitektur-treet, en upresis kildekode-kommentar) → squash-merge. `docs/architecture.md` fikk ny seksjon om identitet/datalagring (inkl. `firestore/lite`-avviket fra issue #18, enhetsbundet anonym identitet, migreringens databevaring), utvidet CSP-tabell og nye kjente forutsetninger/risikoer (Firebase-kvoter). `docs/data-model.md` fikk Firestore-skjemaet. `README.md` fikk en ny "Kjøre lokalt"-seksjon med Firebase-prosjektoppsett. `docs/README.md` (dokumentasjonsindeksen) oppdatert i samme slengen siden den motsa den nye arkitekturteksten.
- **Dermed er hele DB-migreringen (localStorage → Firebase/Firestore, issue #16–#20 / PR #21–#25) ferdig og verifisert i produksjon.** Watchlisten persisteres nå til Firestore per anonym, enhetsbundet Firebase-identitet, med `localStorage` som offline-skriveputt og automatisk engangsmigrering av eksisterende lokale data.

- **Issue #26 (opprydding av de to ikke-blokkerende forslagene fra verifier på issue #20) løst via PR #27**, opprettet direkte i Ready uten planleggingsrunde (triviell, veldefinert). Lagt til de to manglende E2E-spec-filene i `docs/architecture.md`s prosjektstruktur-tre, og rettet en selv-motsigende kommentar i `migrateLocalWatchlistToCloud.ts`. Reelt funn underveis: issue-teksten selv antok feilaktig at migreringen bruker `setDoc` med `merge: true` per felt — dev sjekket faktisk kode (`FirestoreWatchlistStorage.upsert`) og fant at `setDoc` kalles uten `merge: true`, altså full dokumentoverskriving, i tråd med `docs/architecture.md`. Kommentaren ble rettet til å stemme med koden, ikke med issue-tekstens (feilaktige) ordlyd — reviewer og verifier bekreftet begge dette selvstendig mot koden. Godkjent uten funn → squash-merge.

- **Tre uavhengige UI-justeringer (issue #28–#30) satt i Ready samtidig** etter brukerønske, og **kjørt som tre parallelle agent-loops** på eksplisitt brukerforespørsel («løs i parallelle sesjoner») — første gang flere `dev`-agenter har kjørt samtidig i dette prosjektet. Siden `CLAUDE.md`/`dev.md` forutsetter delt arbeidskatalog («kun én agent berører repoet om gangen»), ble hver `dev`-agent spawnet med `isolation: "worktree"` (egen `git worktree` per agent) for å gjøre parallelliteten trygg. `reviewer`/`verifier`-agentene ble derimot spawnet uten isolasjon (delt hovedkatalog) — dette forårsaket observert flakiness da to slike kjørte samtidig (branch byttet under en annen agents føtter, kolliderende `vite preview` på port 4173), og hovedsamtalen selv kom i skade for å committere en `dev-log.md`-oppdatering rett på PR #32s branch i stedet for `main` fordi delt-katalogen sto sjekket ut på feature-branchen da kommandoen kjørte (oppdaget umiddelbart, rettet med `git revert`, ingen kode berørt). Alle observerte kollisjoner ble selvoppdaget og korrigert, så sluttresultatene var pålitelige — men konklusjonen er at **isolasjon bør brukes for `reviewer`/`verifier`-agenter også (eller kjøres sekvensielt), og hovedsamtalens egne commits mot `main` bør gjøres via en egen, isolert worktree** (som denne oppføringen selv er et eksempel på) fremfor i den delte katalogen, når flere agenter kjører samtidig.
  - **Issue #30 (watchlist-knappene "Merk som sett"/"Fjern fra watchlist" alltid på samme linje) løst via PR #31**, godkjent uten funn, verifisert grønt → squash-merge. `WatchlistToggleButton` endret fra én `flex flex-wrap`-container til status-tekst i egen rad (`flex-col`) + handlingsknappene i en indre `flex flex-nowrap`-rad, som garanterer at knappene aldri brytes, uansett viewport-bredde.
  - **Issue #29 (dedupliser strømmetjenester på detaljsiden) løst via PR #32**, godkjent uten funn, verifisert grønt → squash-merge. `StreamingProvidersList` slår nå sammen `offers` til én representant per `providerId` (`dedupeOffersByProvider`), velger første tilbud med gyldig `https:`-URL som lenkemål (ellers første tilbud), og tilbudstype-teksten ("Abonnement"/"Leie" osv.) er fjernet. `docs/design.md` punkt 5 trengte ingen endring — beskrev allerede kun «logo/navn per tjeneste».
  - **Issue #28 (søk- og mikrofonknapp fast nederst på forsiden, over NavBar) løst via PR #33**, godkjent uten funn, verifisert grønt → squash-merge. Knapperaden (søk-submit + `VoiceSearchButton`) flyttet ut av normalflyten i `SearchBar.tsx` til samme `fixed inset-x-0 bottom-[78px]`-mønster som `TitleDetailPage`s CTA; selve input-feltet beholder sin `centered`-styrte plassering. Bevisst, dokumentert avvik fra `docs/design-spec/screenshots/02-sokeresultater.png`: raden vises nå i **begge** tilstander (tom-tilstand og etter søk), siden issuens DoD eksplisitt krever visning uavhengig av `isIdle`-tilstand — vurdert og godkjent av både `reviewer` og `verifier`.
  - **Dermed er alle tre parallelle UI-justeringer (issue #28–#30) ferdig, godkjent og verifisert i produksjonsbygget**, uten at noen av de tre kollisjonene i delt arbeidskatalog (se over) endte opp i faktisk kode eller merget historie.

- **Lærdommen fra parallell-runden implementert direkte i prosessdokumentasjonen** (bruker ba eksplisitt om det, committet rett på `main`, ingen kode rørt): `CLAUDE.md` gjør nå isolasjon (`isolation: "worktree"` på Agent-kallet) til **standard** for alle git-opererende agenter — `dev`, `reviewer` og `verifier` likt — fremfor unntaket det var i parallell-runden over. Samme prinsipp gjelder nå eksplisitt hovedsamtalens egne commits mot `main` (f.eks. denne `dev-log.md`-oppføringen): via en midlertidig worktree fra `origin/main` når andre agenter er eller nylig har vært aktive, ikke i den delte hovedkatalogen. `dev.md` og `verifier.md` sine tidligere bastante «arbeidskatalogen deles»-setninger er myknet til å reflektere at isolert worktree nå er normaltilfellet. `reviewer.md` fikk en ny presisering: siden den har `Read`/`Grep` mot lokale filstier i tillegg til `gh pr diff`, skal den foretrekke GitHub-baserte kommandoer for å unngå å lese feil branch sin filinnhold ved en eventuell delt katalog.

**Neste:** Ingen åpne issues i backlog per nå. Overvåke MOTN-kvoten (100 kall/døgn) i faktisk bruk — nye idéer spores som issues i [GitHub-prosjektet](https://github.com/users/jonasols1g/projects/2).

## 2026-07-17

- Endret talesøk fra norsk til engelsk (`lang: 'en-US'` i stedet for `nb-NO`) i `docs/README.md`, `docs/design.md` og `docs/dev-tasks.md`. Ingen kode berørt — talesøk er ikke implementert ennå (fase 8).
- Installerte Node: nvm 0.40.6 via Homebrew (init i `~/.zshrc`, `NVM_DIR=~/.nvm`), deretter `nvm install --lts` → Node v24.18.0 / npm 11.16.0, satt som default. Punkt 1 i `todo.md` er dermed ferdig.
- Gjenopprettet rot-`README.md` som kort prosjektbeskrivelse med peker til `docs/`.
- Committet og pushet dokumentasjonen: `README.md`, `docs/` og `dev-log.md`. Punkt 2 i `todo.md` er dermed ferdig.
- Installerte `gh` (2.96.0) og logget inn som `jonasols1g`. Forsøkte å slå på GitHub Pages, men repoet er privat og gratisplanen støtter ikke Pages for private repoer. Beslutning: punkt 3 utsatt til fase 9 — da velges enten offentlig repo eller GitHub Pro. Fase 1–8 er ikke blokkert.

- Ryddet `todo.md`: løste punkter (Node, dokumentasjons-commit) fjernet — historikken bor her i loggen.
- Satte opp subagent-team i `.claude/agents/` etter diskusjon med bruker (utvidet fra to til fire roller): `feature-planner` (vurderer nye features mot docs, read-only), `dev` (implementerer faser, kode + enhetstester), `reviewer` (diff-review mot docs og DoD, read-only) og `verifier` (kjører tester/bygg/E2E, endrer aldri kode). Bevisst droppet egne agenter for arkitektur, dokumentasjon og UX — dekkes av `feature-planner` og hovedsamtalen.

- Fase 1 (prosjektoppsett) implementert med agent-teamet: `dev` scaffoldet Vite 8 + React 19 + TS 6 med Tailwind v4 (`@tailwindcss/vite`), ESLint (type-checked) + Prettier, Vitest/RTL, Playwright (webServer mot produksjonsbygg), React Router, mappestruktur og CI-workflow uten Pages-deploy. 404-fallbacken ligger i selve build-scriptet, så den gjelder også lokalt og i E2E. `reviewer` godkjente med to småting (malrest `public/icons.svg` slettet, `lang="no"` i `index.html`) som ble fikset i hovedsamtalen; `verifier` bekreftet lint, enhetstest, E2E, build (404.html identisk med index.html) og Prettier grønt.

- CI grønn på push (begge jobber, inkl. E2E). Fase 1 er dermed komplett iht. Definition of done.

- La om agent-arbeidsflyten til PR-basert (`.claude/agents/`): `dev` jobber nå på feature-branch (`feat/fase-N-…`) og åpner PR mot `main`; `reviewer` konkluderer alltid med en kommentar på PR-en via `gh pr comment` («endringer kreves» med funn, eller «godkjent» som verifikasjon) — `gh pr review --approve` går ikke siden PR-ene er fra samme GitHub-bruker. Funn fikses av `dev` på samme branch og re-reviewes, i loop til godkjent. Deretter sjekker `verifier` ut branchen (`gh pr checkout`) og kjører tester/bygg/E2E; ved grønt squash-merger hovedsamtalen PR-en (`gh pr merge --squash --delete-branch`) — én fase blir én commit på `main`, som i fase 1.

- Strammet inn agent-arbeidsflyten etter en gjennomgang: ny `CLAUDE.md` i rota dokumenterer orkestreringen (spawn-rekkefølge, review-runder via SendMessage til samme agent, eskalering til hovedsamtalen ved uenighet etter to runder, merge og etterarbeid). `reviewer` sjekker nå `gh pr checks` først (rød CI = blokkerende funn) og skal ta eksplisitt stilling til bestridte funn. `verifier` bruker grønn CI som bevis for lint/tester/E2E/bygg og bruker lokal tid på å drive den berørte flyten manuelt mot preview-bygget (nå obligatorisk, ikke «ved behov»). `dev` fikk verktøysavgrensning i frontmatter og krav om ordrett testresultat i rapporten. Både `dev` og `verifier` avslutter alltid på `main`, siden arbeidskatalogen deles.

- Fase 2 (domenemodell, `MediaProvider`-interface og `MockMediaProvider`) implementert via PR #1 med full agent-loop: `dev` → `reviewer` (godkjent uten funn) → `verifier` (grønn CI + drev provider-API-et manuelt mot produksjonsbygget, 11/11 sjekker) → squash-merge. Typene ordrett fra `data-model.md`; mock-katalog med 5 titler inkl. null-tilfellene (Solaris uten RT-score, Oppenheimer uten streaming). 20 enhetstester.
- Docs-avvik funnet av `dev`: `MediaProviderError`-snutten i `architecture.md` brukte parameter properties, som `erasableSyntaxOnly: true` i tsconfig forbyr. Implementert med eksplisitte felt (identisk offentlig flate); snutten i docs oppdatert til å matche.

- Fase 3 (cache-lag) implementert via PR #2 med full agent-loop: `dev` → `reviewer` (godkjent uten funn) → `verifier` (grønn CI + drev cache-laget direkte mot produksjonsbygget, 22/22 sjekker) → squash-merge. `LocalStorageCacheStore` med feature-detection/in-memory-fallback, type guard-validering, TTL og quota-eviction (utløpte først, så eldste); `CachingMediaProvider` som dekoratør rundt `MediaProvider`; `normalizeQuery` og versjonerte nøkkelprefikser (`watchlist:v1:cache:` / `watchlist:v1:data:`). 39 nye enhetstester (59 totalt).

- Fase 4 (app-skjelett) implementert via PR #3 med full agent-loop: `dev` → `reviewer` (godkjent uten funn) → `verifier` (grønn CI + drev navigasjon manuelt mot produksjonsbygget, inkl. dyplenke og ukjent sti) → squash-merge. `App.tsx` med `BrowserRouter basename={import.meta.env.BASE_URL}`, `NavBar` og ruter for `/`, `/watchlist`, `/title/:id`, `*`; `MediaProviderContext` koblet til sammensetningsroten i `services/media/index.ts` (`CachingMediaProvider(MockMediaProvider, LocalStorageCacheStore)`).
- Sidefunn fra `dev` underveis: `setupTests.ts` manglet `afterEach(cleanup)` for Testing Library (usynlig til nå fordi ingen tidligere test rendret flere ganger i samme fil) — fikset. `e2e/smoke.spec.ts` fra fase 1 forventet gammel placeholder-heading — oppdatert til å matche ny `HomePage`.

- Fase 5 (søkeside) implementert via PR #4 med full agent-loop: `dev` → `reviewer` (godkjent uten funn) → `verifier` (grønn CI + drev søk manuelt mot produksjonsbygget: treff, tom-tilstand, klikk→detaljside, submit-only) → squash-merge. `SearchBar` + `useMediaSearch` (statusmaskin idle/loading/success/error, `AbortController` avbryter forrige kall og ved unmount) + `SearchResultsGrid`/`SearchResultCard`. Delte `components/common/{LoadingSpinner,EmptyState,ErrorMessage}` (feilkode→tekst ordrett fra design.md) og `components/media/PosterImage` (begge allerede i architecture.md sin mappestruktur). `HomePage` komponerer alt sammen.
- Kjent, ikke-blokkerende funn fra `verifier`: `MockMediaProvider` (fase 2) sine `posterUrl`-verdier peker til `images.example.com`, som ikke finnes — gir `console.error` i nettleseren nå som postere faktisk rendres i UI. Ikke en fase 5-regresjon, men bør rettes (f.eks. et ekte plakat-CDN eller data-URI) før fase 6 (detaljside) og senere fasers watchlist-kort viser flere postere.

- Fase 6 (detaljside) implementert via PR #5 med full agent-loop: `dev` → `reviewer` (godkjent uten funn) → `verifier` (grønn CI + drev detaljside manuelt mot produksjonsbygget mot fullstendig fixture, manglende plakat/RT-score, manglende streaming, serie-fixture og ukjent id) → squash-merge. `useMediaDetails` (auto-hent ved mount/id-endring, `AbortSignal`, `retry()`), `RatingsBadge`, `GenreTags`, `StreamingProvidersList` (tom-tilstand, kun `https:`-lenker klikkbare) og `TitleDetailPage` som komponerer dem. `WatchlistToggleButton` bevisst utelatt til fase 7. Mindre kosmetisk avvik (sentrert tom-tilstand-tekst i strømme-seksjonen) notert av `verifier`, ikke blokkerende.

- Fase 7 (watchlist-funksjonalitet) implementert via PR #6 med full agent-loop: `dev` → `reviewer` (godkjent uten funn) → `verifier` (grønn CI + drev hele flyten manuelt mot produksjonsbygget: legg til fra søkeresultat → «Planlagt» → bytt status til «Sett» → reload → status beholdt → fjern fra detaljside → reload → tomt) → squash-merge. `WatchlistContext` (`useReducer` med `ADD`/`REMOVE`/`SET_STATUS`, lagrer synkront via en `itemsRef`-wrapper i stedet for `useEffect`, pga. `eslint-plugin-react-hooks`s `set-state-in-effect`-regel — vurdert og godkjent av `reviewer` som et akseptabelt avvik fra docs' illustrative pseudokode) og `watchlistStorage` (type guard-validering, in-memory-fallback, skrivefeil-policy: rydd cache før feilmelding, aldri stille tap). `WatchlistToggleButton` integrert på både `SearchResultCard` og `TitleDetailPage`. `detectLocalStorage` i `LocalStorageCacheStore` generalisert og gjenbrukt.

- Fase 8 (talesøk) implementert via PR #7 med full agent-loop, inkl. én review-runde: `dev` → `reviewer` (endringer kreves: `aria-label="Start talesøk"` på `VoiceSearchButton` kolliderte med Playwrights case-insensitive substreng-matching for `getByRole('button', { name: 'Søk' })`, ga 3 røde E2E-tester i `search.spec.ts`/`watchlist.spec.ts`) → fiks via SendMessage til samme `dev` (aria-label endret til «Start/Stopp talegjenkjenning») → `reviewer` (godkjent) → `verifier` (grønn CI + drev tekstsøk/talesøk-fallback manuelt i ekte Chrome, inkl. en simulert `window.SpeechRecognition` som beviste at et `isFinal`-resultat trigger samme `handleSearch`-flyt som tekstsøk) → squash-merge. `useSpeechRecognition` med feature-detection og `lang: 'en-US'`, `VoiceSearchButton` med tydelig deaktivert fallback. Ingen E2E-spec for talesøk, som spesifisert i `dev-tasks.md` (Web Speech API kan ikke stubbes meningsfullt av Playwright).

- Fase 9 (polish) implementert via PR #8, avgrenset ved brukervalg: repoet er privat, gratisplanen støtter ikke Pages for private repoer, og bruker valgte å utsette selve Pages-aktiveringen fremfor å gjøre repoet offentlig eller oppgradere til GitHub Pro nå. `dev` fikk derfor beskjed om å hoppe over «Aktiver GitHub Pages-publisering»-oppgaven og la `.github/workflows/ci.yml`s deploy-konfigurasjon være urørt; resten av fasen ble gjennomført: a11y-fikser (fokus-styling på 404-siden, `aria-controls`/`role="tabpanel"` på `WatchlistTabs`), responsiv-fiks (`self-start` på `WatchlistToggleButton` som strakk seg feilaktig i en flex-forelder), CSP-meta-tag (`default-src 'self'` osv., bevisst ikke forberedt for fase 10s API-domener), og ny `e2e/deep-links.spec.ts`.
- `dev` → `reviewer` (godkjent) → `verifier` fant et reelt funn ved manuell gjennomgang: den nye CSP-en blokkerte de eksisterende `images.example.com`-plakat-URL-ene (kjent siden fase 5 som et ikke-eksisterende domene), som ga et ødelagt bilde-ikon på søkeresultat-kort og detaljside — en ubehandlet tilstand DoD eksplisitt utelukker. Sendt til samme `dev` via SendMessage; fiksen ble `onError`-håndtering i `PosterImage` som faller tilbake til samme "Ingen plakat"-tomtilstand uansett årsak (CSP, DNS, 404), fremfor å utvide CSP-en (som ikke ville løst det visuelle problemet, siden domenet uansett ikke finnes). `verifier` bekreftet fiksen i runde 2 → squash-merge.
- Fase 9 er dermed delvis ferdig: alt unntatt selve Pages-publiseringen og den siste DoD-linjen («dyplenker fungerer i produksjon på Pages») er verifisert og merget. Se `docs/dev-tasks.md` for detaljer om hva som gjenstår når Pages-beslutningen tas.

- Bugfiks (PR #9, ikke en fase): bruker meldte at `npm run dev` viste appen helt uten styling. Rotårsak: fase 9s CSP (`style-src 'self'`) blokkerte Vites dev-server fra å injisere CSS via inline `<style>`-tagger (brukt til HMR) — konsollen viste en eksplisitt CSP-blokkeringsfeil. Produksjonsbygget rammes ikke, siden Vite der bunter CSS til en egen `.css`-fil lastet via `<link>`; derfor fanget ikke fase 9s `verifier` (som testet mot `vite preview`) dette opp. Fiks: CSP-meta-taggen fjernet fra den statiske `index.html`, injiseres nå i stedet av et nytt Vite-plugin (`cspMetaTagPlugin`, `apply: "build"`) som kun kjører ved `vite build` — selve policyen er uendret, kun injeksjonstidspunktet er flyttet. Kjørt gjennom samme `dev` → `reviewer` → `verifier`-loop som fasene, ingen funn i noen av rundene, squash-merget.

- Endring (PR #10, ikke en fase): watchlist-sidens interne rute endret fra `/watchlist` til `/mylist`, etter ønske fra bruker om å unngå den forvirrende doble stien `.../watchlist/watchlist` (appens `base`-understi på GitHub Pages, `/watchlist/`, er uendret — kun sideruten er flyttet). Oppdatert i `App.tsx`, `NavBar`, begge E2E-spec-ene og rutetabellene i `design.md`/`architecture.md`. Kjørt gjennom samme `dev` → `reviewer` → `verifier`-loop, ingen funn, squash-merget. Flyttet også oppgaven om å aktivere GitHub Pages-publisering fra fase 9 til fase 10s oppgaveliste (2026-07-17), siden den uansett venter på Pages-beslutningen og fase 10 er et mer naturlig sted for den.

**Neste:** Pages-beslutningen (offentliggjøre repoet vs. GitHub Pro) må tas før fase 9 kan hakes helt av og fase 10s Pages-oppgave kan gjennomføres.

## 2026-07-16

Prosjektstart. Ingen kode skrevet ennå — dagen gikk med til planlegging og dokumentasjon.

- `first commit` (ee55345): tomt repo med en én-linjes `README.md`.
- Planleggingsrunde med bruker som avklarte omfang: 100 % klient-side webapp for oppslag på film/serie og en personlig watchlist. Enkeltbruker, ingen innlogging, ingen deling.
- Bekreftet teknisk stack: React + TypeScript + Vite, Tailwind, React Router, `localStorage` for både watchlist og cache, Web Speech API (`nb-NO`) for talesøk med fallback til tekstsøk.
- Valgte datakilder: OMDb for søk og titteldata, Movie of the Night for strømmetilgjengelighet. TMDB forkastet. IMDb-ID (`tt0133093`) er felles nøkkel og `Media.id`, så de to API-ene kan kalles parallelt i `getDetails`.
- Verifiserte forutsetninger med `curl` i stedet for å anta: både OMDb og MOTN svarer over https med `access-control-allow-origin: *`, og MOTN-preflight godtar `X-API-Key`. Dermed holder «ingen backend» — ingen proxy trengs.
- Landet på `MediaProvider`-abstraksjonen med `CachingMediaProvider` og `CompositeMediaProvider`, slik at fase 1–9 kan bygges mot en `MockMediaProvider` og ekte API-er kobles på først i fase 10.
- La til Playwright E2E i teststrategien etter korrigering fra bruker — E2E kjører mot stubbet nettverk og produksjonsbygg, aldri mot ekte API-er (MOTN-kvoten er 100 kall/døgn).
- Sjekket maskin- og repo-status: Node er **ikke** installert (verken `node`, `npm`, `nvm` eller via Homebrew), `gh` mangler også. GitHub-repoet finnes og `main` er pushet.
- Skrev dokumentasjonen i `docs/`: `README.md`, `architecture.md`, `data-model.md`, `design.md`, `dev-tasks.md` og `todo.md`. Rot-`README.md` ble flyttet inn dit. `todo.md` lister de tre tingene som blokkerer fase 1: installer Node, commit dokumentasjonen, slå på GitHub Pages med Source: GitHub Actions.

**Neste:** `todo.md` i rekkefølge, deretter fase 1 i `dev-tasks.md`.
